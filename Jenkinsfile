pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add your build steps here
                sh 'mkdir /home/ec2-user/webhook' // Example for a makefile project
            }
        }
    }
}
