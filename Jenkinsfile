pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add your build steps here
                sh 'mkdir ~/webhook' // Example for a makefile project
            }
        }
    }
}
